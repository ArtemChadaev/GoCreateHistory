# Все связяноое с запросами

Так сделать jwt токен для простоты

проверять историю только пингуя по ней

Так все параметры которые надо добавить в запросы по созданию истории
- save (bool) сохранять ли эту картинку или при получении сразу удалить или через время только удалить её и оставить тольк сид например. Так сделать для усложнения чтобы это мого быть или число или bool по стандарту будет false (в ближайшие 12 (в env сделать переменную) часа тоже удалить) true - бессрочно сохранять, int - время в минутах для сохранения (если больше суток то в бд иначе оставить в redis), проверки что больше 0 и т.д.
- uuid (запроса)


Все ошибки логируются в handler до этого просто переносим

Используем slog для логов